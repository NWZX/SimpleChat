version: "3"
services:
    app:
        container_name: simple_chat
        restart: always
        build: .
        ports:
            - "8000:3000"
        links:
            - mongo
    mongo:
        container_name: mongo
        image: mongo
        ports:
            - "27018:27017"